{
  "name": "playwright-test-framework",
  "version": "1.0.0",
  "description": "Extensible Playwright test framework with API and UI wrappers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:api": "playwright test src/tests/api",
    "test:integration": "playwright test src/tests/integration",
    "test:retry": "RETRY_COUNT=3 playwright test src/tests/integration/retry-example.test.ts",
    "test:retry-ui": "RETRY_COUNT=3 playwright test src/tests/ui/login-with-retry.ui.test.ts",
    "test:no-retry": "RETRY_COUNT=0 playwright test",
    "test:max-retry": "RETRY_COUNT=5 playwright test",
    "test:report": "playwright show-report",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write src/**/*.ts",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "docker:build": "./build.sh",
    "docker:test": "./docker-run.sh test",
    "docker:test:api": "./docker-run.sh test-api",
    "docker:test:ui": "./docker-run.sh test-ui --ui",
    "docker:test:integration": "./docker-run.sh test-integration",
    "docker:shell": "./docker-run.sh shell",
    "docker:clean": "./docker-run.sh clean",
    "docker:report": "./docker-run.sh report",
    "setup:all": "./build.sh --local",
    "setup:docker": "./build.sh",
    "setup:local": "./build.sh --local --skip-docker"
  },
  "keywords": [
    "playwright",
    "testing",
    "automation",
    "typescript",
    "framework"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.53.2",
    "@types/node": "^24.0.8",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.8.3"
  },
  "files": [
    "dist",
    "src",
    "playwright.config.ts",
    "README.md"
  ],
  "engines": {
    "node": ">=16.0.0"
  }
}
